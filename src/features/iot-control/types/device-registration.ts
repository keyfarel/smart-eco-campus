export interface RegisteredDevice {
  macAddress: string
  buildingId: string
  buildingName: string
  floorId: string
  roomName: string
  capacity: number
  cameraStreamUrl: string
  createdAt: number
}
